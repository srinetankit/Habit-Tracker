<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/signup.css">
    <title><%= title %></title>
</head>
<body>
    <!-- for flash message -->
    <!-- <div id="flash-msg" class="flash-msg">
        <p>
            <% if (flash.error && flash.error.length > 0) { %>
                <span class="error"><%= flash.error %>&nbsp;&nbsp;<i class="fa-solid fa-xmark" onclick="hideFlash()"></i></span>
            <% } %>
            <% if (flash.success && flash.success.length > 0) { %>
                <span class="success"><%= flash.success %>&nbsp;&nbsp;<i class="fa-solid fa-xmark" onclick="hideFlash()"></i></span>
            <% } %>
        </p>
    </div> -->

    <div class="background">
        <div class="shape"></div>
        <div class="shape"></div>
    </div>
    <form action="/users/create" method="POST">
        <h3>Sign Up for Track Your Habit</h3>

        <label for="username">User name</label>
        <input type="text" name="name" id="username" placeholder="Your Name" required>

        <label for="email">Email</label>
        <input type="text" name="email" id="email" placeholder="yourmail@gmail.com" required>

        <label for="password">Password</label>
        <input type="password" name="password" placeholder="Password" id="password" required>

        <label for="cp">Confirm Password</label>
        <input type="password" name="confirm_password" placeholder="Confirm Password" id="cp" required>

        <input type="submit" value="Sign Up">
        <h3 id="already">Already have account ? <a href="/users/sign-in" id="signinlink">Sign In</a></h3>
    </form>

    <script src="/js/script.js"></script>
    
</body>
</html>